import { createContext } from "react";

// arquivo onde o contexto foi criado. Exportado para App, onde recebe o valor (value), podendo recuperá-lo em qualquer componente
export const UserContext = createContext();