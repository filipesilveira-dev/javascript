function soma(n1=0, n2=0){ // dessa forma, caso não seja especificado um dos números (n1 ou n2), será considerado como zero
    return n1 + n2  // retorna a soma dos dois valores
}

console.log(soma(2, 7)) // a chamada atribui os valores 2 e 7 respectivamente